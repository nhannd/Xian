using System.Windows.Forms;

namespace ClearCanvas.Desktop.View.WinForms
{
	//could just use PropertyGetDelegate, but then I'd have to include Common.
	public delegate ContextMenuStrip GetContextMenuStripDelegate();
}