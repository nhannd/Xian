using System;
using System.Collections.Generic;
using System.Text;
using ClearCanvas.Desktop;

namespace ClearCanvas.ImageViewer.Imaging
{
	/*
	/// <summary>
	/// An interface that allows for cyclic application of Voi Luts to an image.  Generally,
	/// these Luts would come from, say, a Dicom Header.
	/// </summary>
	public interface IAutoVoiLutApplicator : IMemorable
	{
		/// <summary>
		/// Each call to this method should apply the next Voi Lut in a known sequence, if any.
		/// Preferably, if none exist, a default one should be determined and applied.
		/// </summary>
		void ApplyNext();
	}*/
}
